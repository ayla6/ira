[package]
name = "ira-config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xdg = "3.0.0"
ira-models = { path = "../models" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
