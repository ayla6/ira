[package]
name = "ira-launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
shlex = "1.3"
which = "7.0"
libc = "0.2"
ira-models = { path = "../models" }
ira-db = { path = "../db" }
ira-overlay-ipc = { path = "../overlay-ipc" }
ira-input-ipc = { version = "0.1.0", path = "../input-ipc" }
ira-input = { version = "0.1.0", path = "../input" }
ira-parser = { version = "0.1.0", path = "../parser" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
