[package]
name = "ira-overlay-ipc"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
libc = "0.2"

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
