[package]
name = "ira-overlay"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
ira-overlay-ipc = { path = "../overlay-ipc" }
ash = "0.38"
libc = "0.2"
webp = { version = "0.3", default-features = false }

[lints]
workspace = true
