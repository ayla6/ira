[package]
name = "ira-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25", features = ["png", "ico", "jpeg", "webp", "tga"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
webp = "0.3"
chrono = { version = "0.4", default-features = false, features = ["std"] }
tracing = "0.1"
ira-models = { path = "../models" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
